machine:
  python:
    version: 3.5.2
dependencies:
  pre:
    - pip install codecov
test:
  override:
    - coverage run --source=dusken,apps manage.py test -v 2
  post:
    - bash <(curl -s https://codecov.io/bash)